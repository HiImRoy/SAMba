### **项目简报: SAMbaCrack-AF 模型架构与数据流详解 (U-Net Decoder 版)**

你好 Gemini，这是 SAMbaCrack-AF 模型的**最终设计蓝图**。此版本已将解码器升级为 **U-Net 风格架构**，以实现更精细的多尺度特征融合。请你仔细阅读并完全理解这个新架构。后续我们所有的讨论和编码都将以此文档为准。

---

#### **第一部分: 编码器 (Backbone) - SAMbaCrackEncoder**
*(此部分保持不变，以提供完整上下文)*

**职责**: 模型的特征提取引擎，由两个完全独立、并行的分支组成。

*   **1.1 SAM/Hiera 分支 (冻结的语义编码器)**
    *   **角色**: 利用强大的预训练权重，提供具有丰富高级语义信息的特征。其参数在训练中被冻结。
    *   **输入**: 原始图像 `(B, 3, 448, 448)`。
    *   **结构**: 层级式视觉 Transformer (Hiera)。
    *   **输出 (`feats_sam`)**: 一个包含4个多尺度特征图（经过 Refiner 和 Adapter 处理）的元组：
        *   `[0]`: (B, 96, 112, 112)
        *   `[1]`: (B, 192, 56, 56)
        *   `[2]`: (B, 384, 28, 28)
        *   `[3]`: (B, 768, 14, 14)

*   **1.2 SAVSS/Mamba 分支 (可训练的细节编码器)**
    *   **角色**: 从头学习任务特定的特征，特别是裂缝的纹理、走向等细节信息。
    *   **【关键架构】**: 这是一个**真正的层级式金字塔编码器**，与 SAM/Hiera 分支在结构上对等。
    *   **数据流**:
        *   **Stage 1**: `PatchEmbed (stride=4)` 将 `(B, 3, 448, 448)` 降采样至 `(B, 96, 112, 112)`，然后通过 `SAVSS_Block` 组进行特征提取。
        *   **Stage 2**: `Downsample` 模块将特征降采样至 `(B, 192, 56, 56)`，再通过 `SAVSS_Block` 组处理。
        *   ...以此类推，完成所有四个阶段。
    *   **输出 (`feats_mamba`)**: 一个包含4个特征图的元组，其维度与 `feats_sam` 完全匹配。

---

#### **第二部分: 颈部 (Neck) - AFNeck**
*(此部分保持不变)*

**职责**: 模型的“脊柱”，负责将来自两个主干网络的特征进行智能、自适应的融合。
*   **输入**: `feats_sam` 和 `feats_mamba` 两个并行的特征金字塔。
*   **结构**: 包含4个并行的 `LightAdaptiveFusionModule`。
*   **输出 (`fused_feats`)**: 一个单一的、融合后的特征金字塔，维度与输入金字塔完全相同 `[(B, 96, 112, 112), ..., (B, 768, 14, 14)]`。

---

#### **第三部分: 解码器头 (Head) - UNetDecoderHead**
**(【核心修改】)**

**职责**: 将来自颈部的融合特征金字塔，通过**逐级上采样和跳跃连接**的方式，进行精细的解码，最终生成全分辨率的像素级预测。

*   **输入**: `fused_feats` 元组 `(c1, c2, c3, c4)`，其中 `c1` 是 `(B, 96, 112, 112)`。

*   **内部数据流与维度变化**:
    *   **A. Stage 4 (最底层)**:
        *   输入 `c4` (`B, 768, 14, 14`)。
        *   通过一个 `ConvBlock` 进行深度特征提取，输出 `d4` (`B, 768, 14, 14`)。
    *   **B. Stage 3 (上采样与融合)**:
        *   `d4` 经过 `up4` (`ConvTranspose2d`) 上采样至 `(B, 384, 28, 28)`。
        *   与来自 `fused_feats` 的 `c3` (`B, 384, 28, 28`) 进行拼接，得到 `d3_cat` (`B, 768, 28, 28`)。
        *   将 `d3_cat` 送入 `ConvBlock` 进行融合，输出 `d3` (`B, 384, 28, 28`)。
    *   **C. Stage 2 (上采样与融合)**:
        *   `d3` 经过 `up3` 上采样至 `(B, 192, 56, 56)`。
        *   与 `c2` (`B, 192, 56, 56`) 拼接，得到 `d2_cat` (`B, 384, 56, 56`)。
        *   送入 `ConvBlock` 融合，输出 `d2` (`B, 192, 56, 56`)。
    *   **D. Stage 1 (上采样与融合)**:
        *   `d2` 经过 `up2` 上采样至 `(B, 96, 112, 112)`。
        *   与 `c1` (`B, 96, 112, 112`) 拼接，得到 `d1_cat` (`B, 192, 112, 112`)。
        *   送入 `ConvBlock` 融合，输出 `d1` (`B, 96, 112, 112`)。

*   **E. 优化的最终输出策略**:
    *   **通道降维**: `d1` 经过一个 `1x1 Conv`，将通道从 96 压缩至 **16**，得到 `(B, 16, 112, 112)`。
    *   **动态上采样**: 使用 `DySample` 模块将这个 16 通道的特征图上采样 4 倍至全分辨率，得到 `(B, 16, 448, 448)`。
    *   **最终预测**: 在全分辨率上，使用最后一个 `1x1 Conv` 将 16 通道特征图投影到最终的 **1 通道** logits。

*   **输出 (`logits`)**: 直接输出最终的全分辨率预测图 `(B, 1, 448, 448)`。

---

#### **第四部分: 最终输出与损失计算**
*(此部分逻辑保持不变，仅更新描述)*

*   **最终输出**: `UNetDecoderHead` 的输出 `logits` 已经是全分辨率，不再需要额外的上采样步骤。
*   **损失计算**: `logits` (`B, 1, 448, 448`) 直接与 Ground Truth (`B, 1, 448, 448`) 一起送入损失函数（例如 Focal Loss + Dice Loss）。

---
